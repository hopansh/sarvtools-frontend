import { defaultConfig } from './defaultConfig';
import { hindiConfig } from './defaultHindiConfig';
import { devConfig } from './devConfig';


export const siteConfigs = {
  dev: devConfig,
  default: defaultConfig,
  default_hi: hindiConfig,
};
